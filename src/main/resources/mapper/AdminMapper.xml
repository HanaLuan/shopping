<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.quanweng.shopping.mapper.AdminMapper">


    <insert id="createAdmin" useGeneratedKeys="true" keyProperty="id">
        insert into admin_table(admin_name, admin_password, admin_level, admin_remark, admin_from, create_time, update_time)
        VALUES(#{adminName},#{adminPassword},#{adminLevel},#{adminRemark},#{adminFrom},#{createTime},#{updateTime})
    </insert>
    <update id="updateAdmin">
        update admin_table
        <set>
            admin_name = #{adminName},
            <if test="adminPassword != null">
                admin_password = #{adminPassword},
            </if>
            admin_level = #{adminLevel},
            <if test="adminRemark != null">
                admin_remark = #{adminRemark},
            </if>
            <if test="adminStatus != null">
                admin_status = #{adminStatus},
            </if>
            admin_from = #{adminFrom},
            update_time = #{updateTime}
        </set>
        where
            id = #{id}
    </update>
    <delete id="deleteAdmin">
        delete from admin_table where id = #{id}
    </delete>
    <select id="findTheLogin" resultType="com.quanweng.shopping.pojo.Admin">
        select * from admin_table where admin_name = #{adminName} and admin_password = #{adminPassword} order by update_time desc limit 1
    </select>
</mapper>
