<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.quanweng.shopping.mapper.GoodsTopMapper">

    <insert id="createGoodsTop">
        insert into goods_top_table(goods_id, goods_count, create_time, update_time)
        values (#{goodsId},#{goodsCount},#{createTime},#{updateTime})
    </insert>
    <update id="updateGoodsTop">
        update goods_top_table
        set
            goods_count = #{goodsCount},
            update_time = #{updateTime}
        where
            id = #{id}
    </update>
</mapper>
