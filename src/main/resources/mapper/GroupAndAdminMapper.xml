<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.quanweng.shopping.mapper.GroupAndAdminMapper">

    <insert id="addGroupAndAdmin">
        insert into group_and_admin_table(group_id, admin_id, create_time, update_time)
        values (#{groupId},#{adminId},#{createTime},#{updateTime})
        </insert>
    <delete id="deleteGroupAndAdmin">
        delete from group_and_admin_table where group_id = #{groupId} and admin_id = #{adminId}
    </delete>
</mapper>
