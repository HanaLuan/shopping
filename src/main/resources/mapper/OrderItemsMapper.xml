<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.quanweng.shopping.mapper.OrderItemsMapper">

    <insert id="createOrderItems">
        insert into order_items_table(order_id, item_id, item_qty, create_time, update_time)
        VALUES(#{orderId},#{itemId},#{itemQty},#{createTime},#{updateTime})
    </insert>
    <update id="updateOrderItems">
        update order_items_table
        set
            order_id = #{orderId},
            item_id = #{itemId},
            item_qty = #{itemQty},
            update_time = #{updateTime}
        where
            id = #{id}
    </update>
</mapper>
