<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.quanweng.shopping.mapper.CategoryImgMapper">

    <insert id="createCategoryImg">
        insert into category_img_table(category_id, category_img, create_time, update_time)
        VALUES(#{categoryId},#{categoryImg},#{createTime},#{updateTime})
    </insert>
    <update id="updateCategoryImg">
        update category_img_table
        set
            category_id = #{categoryId},
            category_img = #{categoryImg},
            update_time = #{updateTime}
        where
            id = #{id}
    </update>
    <delete id="deleteCategoryImg">
        delete from category_img_table where id = #{id}
    </delete>

</mapper>
