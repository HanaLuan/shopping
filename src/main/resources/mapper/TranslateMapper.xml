<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.quanweng.shopping.mapper.TranslateMapper">

    <insert id="createTranslation">
        insert into translate_table(text, lang, translate_text, create_time, update_time)
        VALUES (#{text},#{lang},#{translateText},#{createTime},#{updateTime})
    </insert>
    <update id="updateTranslation">
        update translate_table
        set
            text = #{text},
            lang = #{lang},
            translate_text = #{translateText},
            update_time = #{updateTime}
        where
            id = #{id}
    </update>
    <delete id="deleteTranslation">
        delete from translate_table where text = #{text}
    </delete>
</mapper>
