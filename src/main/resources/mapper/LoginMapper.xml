<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.quanweng.shopping.mapper.LoginMapper">

    <insert id="register">
        insert into login_table(phone, password, create_time, update_time)
        VALUES(#{phone},#{password},#{createTime},#{updateTime})
    </insert>
    <select id="findTheLogin" resultType="com.quanweng.shopping.pojo.Login">
        select * from login_table where phone = #{phone} and password = #{password}
    </select>
</mapper>
