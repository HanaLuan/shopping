<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.quanweng.shopping.mapper.LoginMapper">

    <insert id="register">
        insert into login_table(phone, password, admin_id ,create_time, update_time)
        VALUES(#{phone},#{password},#{adminId},#{createTime},#{updateTime})
    </insert>
    <update id="updateLogin">
        update login_table
        set
        <if test="phone != null">
            phone = #{phone},
        </if>
        <if test="password != null">
            password = #{password},
        </if>
        <if test="updateTime != null">
            update_time = #{updateTime}
        </if>
        where
            admin_id = #{adminId}
    </update>
    <select id="findTheLogin" resultType="com.quanweng.shopping.pojo.Login">
        select * from login_table where phone = #{phone} and password = #{password}
    </select>
</mapper>
